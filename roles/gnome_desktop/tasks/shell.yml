---

- name: "Gnome Classic Session"
  package:
    name: "{{ wtd_gd_classic_package_name }}"
    state: "{{ wtd_gd_classic_package_state }}"
  become: true
  tags:
    - "gnome"
    - "extension"
    - "shell"
    - "package"